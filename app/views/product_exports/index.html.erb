<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Back to Home", root_path, class: "text-blue-600 hover:text-blue-800" %>
  </div>

  <h1 class="text-3xl font-bold mb-6">Product Exports</h1>
  
  <div class="mb-4 flex flex-wrap gap-4">
    <%= link_to "View Products", products_path, class: "text-blue-600 hover:text-blue-800" %>
    <span class="text-gray-400">|</span>
    <%= link_to "View DataFlow", data_flow_path, class: "text-blue-600 hover:text-blue-800" %>
    <span class="text-gray-400">|</span>
    <%= link_to "Trigger Heartbeat", heartbeat_path, method: :post, class: "text-blue-600 hover:text-blue-800" %>
  </div>

  <div class="bg-white shadow-md rounded-lg overflow-hidden">
    <table class="min-w-full">
      <thead class="bg-gray-100">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Product ID</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">SKU</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Price (cents)</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Category Slug</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Exported At</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% @product_exports.each do |export| %>
          <tr>
            <td class="px-6 py-4 whitespace-nowrap"><%= export.product_id %></td>
            <td class="px-6 py-4 whitespace-nowrap"><%= export.sku %></td>
            <td class="px-6 py-4 whitespace-nowrap"><%= export.name %></td>
            <td class="px-6 py-4 whitespace-nowrap"><%= export.price_cents %></td>
            <td class="px-6 py-4 whitespace-nowrap"><%= export.category_slug %></td>
            <td class="px-6 py-4 whitespace-nowrap"><%= export.exported_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <% if @product_exports.empty? %>
      <div class="p-6 text-center text-gray-500">
        No exports yet. Trigger the DataFlow to sync products.
      </div>
    <% end %>
  </div>
</div>
